{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      // Aliases for easier imports
      "@/*": ["./src/*"],
      "@assets/*": ["./assets/*"],
    }
  }
}
